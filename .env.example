PORT=3334
DATABASE_PATH=./data/app.db
SESSION_SECRET=change-this-to-a-random-secret
JETSTREAM_URL=wss://jetstream2.us-east.bsky.network/subscribe

# OAuth Configuration (required for OAuth login)
# PUBLIC_URL must be the publicly accessible URL of your app (e.g., https://yourdomain.com)
# Leave unset to disable OAuth and use only app password login
# PUBLIC_URL=https://yourdomain.com

# Test credentials for integration tests (optional)
TEST_HANDLE=your-handle.bsky.social
TEST_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# ============================================
# PDS (Personal Data Server) Configuration
# ============================================
# Enable social login by configuring at least one provider below.
# When enabled, users can sign in with GitHub/Google and get their own
# ATProto identity hosted on this server.

# PDS Hostname - the domain where handles will be created (e.g., alice.leaf.pub)
# PDS_HOSTNAME=leaf.pub
# PDS_HANDLE_DOMAIN=leaf.pub

# PDS Secrets (uses SESSION_SECRET by default, but can be separate)
# PDS_JWT_SECRET=change-this-to-a-random-secret
# PDS_KEY_ENCRYPTION_SECRET=change-this-to-another-random-secret

# GitHub OAuth (get credentials at https://github.com/settings/developers)
# GITHUB_CLIENT_ID=your-github-client-id
# GITHUB_CLIENT_SECRET=your-github-client-secret

# Google OAuth (get credentials at https://console.cloud.google.com/apis/credentials)
# GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# PLC Directory URL (default: https://plc.directory)
# PLC_DIRECTORY_URL=https://plc.directory
